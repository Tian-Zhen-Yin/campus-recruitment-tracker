# 校招投递台账（Campus Recruitment Tracker）

求职者管理校招投递的主界面与数据主副本。数据保存在用户浏览器本地；官网状态由本机控制台抓取后由台账合并。

## Language

### 界面与角色

**台账**：
用户日常使用的主界面，也是投递记录、岗位库与绑定关系的主副本所在地（浏览器本地存储）。
_Avoid_: 管理器、聚合工作表、前端

**控制台**：
本机后台服务，托管台账页面并驱动官网抓取、定时任务与通知；不保存任何台账数据，抓取结果只是缓存。
_Avoid_: 引擎面板、聚合工具、服务端（面向用户的界面中一律不出现）

### 自动跟进

**接入**：
让控制台获得某家公司官网的登录态，从而能够读取该公司的「我的投递」。
_Avoid_: 登录会话、抓包、配置接口、常驻

**官网进展**：
控制台从公司官网抓到的、某条投递的最新状态。
_Avoid_: 状态包、同步数据、抓取结果

**绑定**：
一条台账记录与一条官网条目之间的一对一对应关系；绑定后该记录的状态以官网为准自动更新，不再依赖名字匹配。
_Avoid_: 同步、关联（指代绑定时）

**拍板**：
官网进展与台账记录冲突、或无法自动对应时，由用户决定以谁为准、是否收录。拍板只发生在用户主动打开的待办场景，绝不在后台静默改数据。
_Avoid_: 确认队列、裁决、同步应用（面向用户的文案中）
